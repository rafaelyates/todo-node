{
  "$schema": "https://json.schemastore.org/nodemon",
  "restartable": "rs",
  "watch": ["src/"],
  "execMap": {
    "ts": "node --import tsx/esm -r esbuild-register -r tsconfig-paths/register"
  },
  "env": {
    "NODE_ENV": "development"
  },
  "ext": "json,ts"
}
